{{- if .Values.migrations.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  generateName: scheduling-migrate-
  namespace: {{ include "clinicflow.namespace" . }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        job: scheduling-migrate
        {{- include "clinicflow.labels" . | nindent 8 }}
    spec:
      restartPolicy: Never

      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}

      initContainers:
        - name: wait-for-db
          image: postgres:16-alpine
          command: ["sh", "-lc"]
          args:
            - |
              echo "Waiting for scheduling-db..."
              for i in $(seq 1 180); do
                pg_isready -h scheduling-db -p 5432 && exit 0
                sleep 2
              done
              echo "Timed out waiting for scheduling-db"
              exit 1

      containers:
        - name: migrate
          image: "{{ .Values.images.scheduling.repository }}:{{ .Values.images.scheduling.tag }}"
          imagePullPolicy: IfNotPresent
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: scheduling-db-secret
                  key: DATABASE_URL
          command: ["sh", "-lc"]
          args:
            - "./node_modules/.bin/prisma migrate deploy"
{{- end }}
