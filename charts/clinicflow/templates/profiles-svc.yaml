apiVersion: v1
kind: Service
metadata:
  name: clinicflow-profiles
  namespace: {{ include "clinicflow.namespace" . }}
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  selector:
    app: clinicflow-profiles
  ports:
    - name: http
      port: {{ .Values.ports.profiles }}
      targetPort: {{ .Values.ports.profiles }}
